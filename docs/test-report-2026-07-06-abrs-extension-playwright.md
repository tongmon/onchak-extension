# ABRS Extension Playwright Test Report - 2026-07-06

## Scope

Implemented and verified the first OnChak Extension ABRS upload slice:

- Popup ABRS ledger upload card.
- Target date selection.
- One-by-one workbook attachment stacking.
- Duplicate workbook slot replacement.
- Date validation using `Statistics` and `dailySettlement` filenames.
- `inventory_health_sku_info_*.xlsx` timestamp ignored as an accounting date.
- Existing OMS upload contract reuse: `POST /api/ledger/imports`.
- Coupang Wing/Ads page detection bridge for automatic download automation.
- Coupang Wing inventory-health workbook auto fetch bridge.
- Coupang Wing sales-statistics workbook auto fetch bridge.
- Coupang Ads daily-settlement workbook auto fetch bridge.
- Playwright persistent Chrome profile launcher for real Coupang login reuse.

## Commands And Results

```bash
cd /Users/admin/refacoring_project/saiload/onchak-extension
npm test
```

Result: PASS, 28 tests.

```bash
npm run typecheck
```

Result: PASS.

```bash
npm run build
```

Result: PASS. Extension `dist/manifest.json` generated successfully.

```bash
npx playwright install chromium
```

Result: PASS. Chromium v1228 installed under the local Playwright cache.

```bash
npm run test:e2e:abrs
```

Result: PASS, 1 Playwright smoke test.

Verified behavior:

- Extension popup opens from built `dist`.
- ABRS section is visible after test auth session bootstrap.
- Target date can be set to `2026-04-18`.
- Three workbook files can be attached one by one.
- All three slots remain visible and stacked.
- `서버 업로드` becomes enabled after required files are present.
- Popup still renders after adding the Wing auto-download buttons.
- Popup renders the Ads auto-download button for `dailySettlement`.

```bash
ONCHAK_ABRS_START_URL=about:blank npm run playwright:abrs:profile
```

Result: PASS for launcher startup. Browser opened with the built extension and persistent profile:

```text
/Users/admin/.playwright-profiles/onchak-coupang-wing
```

The command was stopped with Ctrl+C after startup verification.

## Real-Site Verification

Logged-in Coupang Wing discovery was performed with the persistent Playwright profile.

Confirmed pages:

- `https://wing.coupang.com/`
- `https://wing.coupang.com/tenants/rfm-inventory/management/list`
- `https://advertising.coupang.com/marketing/dashboard/sales`

Confirmed account text in the live browser:

- `Coupang Wing - 강민성, 제플리(zephly)`
- Advertising account code: `A01549099`

Confirmed real workbook download artifacts:

- `test-results/real-coupang-discovery/downloads-live-login/inventory_health_sku_info_20260706223816.xlsx`
- `test-results/real-coupang-discovery/downloads-force/Statistics-20260706~20260706_(0).xlsx`

Confirmed Wing endpoints from live network/bundle discovery:

- Inventory health workbook: `POST /tenants/rfm-inventory/inventory-health-dashboard/excel-report`
- Sales workbook request: `POST /fcc/download-manager/request-auto-download`
- Sales workbook poll: `GET /fcc/download-manager/check-downloadable?requestId=...`
- Sales workbook download: `GET /fcc/download-manager/download/{requestId}/{downloadId}`
- Daily settlement query: `POST /marketing-reporting/v2/graphql` with `getDailySettlement(startDate, endDate, settlementDomain: SELLER)`
- Daily settlement xlsx: generated by the extension from the live Ads GraphQL payload because Coupang Ads creates the download client-side as a browser Blob.

Implemented extension bridge:

- Runtime message: `abrs/download-active-tab-ledger-file`
- Tab message: `abrs/download-ledger-file`
- Popup buttons for `inventoryHealth` and `salesStatistics`
- Popup button for `dailySettlement`

Latest endpoint smoke attempt:

```bash
node --input-type=module <extension-endpoint-smoke>
```

Initial result: BLOCKED by Coupang login in the Playwright profile. The browser reached:

```text
https://xauth.coupang.com/auth/realms/seller/protocol/openid-connect/auth...
```

Follow-up: the login form already had saved credentials. After clicking `input[type="submit"]`, the browser entered Wing:

```text
Coupang Wing - 강민성, 제플리(zephly)
```

Endpoint smoke after the login click:

- Inventory response: `200`, `inventory_health_sku_info_20260706225558.xlsx`, xlsx zip magic present.
- Sales response: `200`, `Statistics-20260706~20260706_(0).xlsx`, xlsx zip magic present.
- Saved local evidence:
  - `test-results/real-coupang-discovery/extension-endpoint-smoke-after-login-click/inventory_health_sku_info_20260706225558.xlsx`
  - `test-results/real-coupang-discovery/extension-endpoint-smoke-after-login-click/Statistics-20260706~20260706_(0).xlsx`

Extension bridge smoke after the filename decode fix:

- Extension ID: `badehielmafamdjagofjjanpdooonfpb`
- Inventory content-script response: `inventory_health_sku_info_20260706225743.xlsx`, base64 xlsx payload returned.
- Sales content-script response: `Statistics-20260706~20260706_(0).xlsx`, base64 xlsx payload returned.
- Saved local evidence:
  - `test-results/real-coupang-discovery/extension-bridge-smoke-after-filename-fix/summary.json`

Ads daily settlement real UI flow:

- Tried `2026-07-06` first; Coupang Ads returned an empty settlement view, so no xlsx was downloaded.
- Re-ran the user-provided UI flow with `2026-04-18`:
  - `광고센터` login
  - left menu `광고보고서`
  - top tab `광고비 정산 보고서`
  - dropdown `일별 광고비 정산내역 보고서`
  - `기간 설정`
  - start/end date `2026-04-18`
  - `보고서 만들기`
  - `엑셀파일 다운로드`
- Downloaded real file:
  - `test-results/real-coupang-discovery/ads-daily-settlement-real-dates/2026-04-18/A01549099-dailySettlement-20260418-20260418.xlsx`
- Screen evidence:
  - `test-results/real-coupang-discovery/ads-daily-settlement-real-dates/2026-04-18/09-after-excel-click.png`
- Live values displayed by Coupang Ads:
  - 소진 광고비: `1,209원`
  - 청구금액(+부가가치세): `1,330원`

Extension content-script daily settlement smoke:

- Active tab: `https://advertising.coupang.com/marketing-reporting/billboard/settlements/daily`
- Extension ID: `badehielmafamdjagofjjanpdooonfpb`
- Request: `abrs/download-ledger-file`, slot `dailySettlement`, target date `2026-04-18`
- Result: `ok: true`
- Returned file: `A01549099-dailySettlement-20260418-20260418.xlsx`
- MIME type: `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- Generated byte length: `8481`
- Zip magic: `[80, 75, 3, 4]`
- Saved local evidence:
  - `test-results/real-coupang-discovery/extension-content-daily-settlement-20260418/summary.json`
  - `test-results/real-coupang-discovery/extension-content-daily-settlement-20260418/A01549099-dailySettlement-20260418-20260418.xlsx`

Backend compatibility smoke:

```bash
cd /Users/admin/refacoring_project/saiload/Refactor_OMSServer
./gradlew :app:test --tests 'com.saiload.oms.adapter.in.web.ledger.LedgerImportApiTest'
```

Result: PASS. Existing ledger import API/parser tests still parse `COUPANG_DAILY_SETTLEMENT` files and the mixed ABRS upload flow.

Local running-server upload smoke:

```bash
cd /Users/admin/refacoring_project/saiload/Refactor_OMSServer
./gradlew :app:bootRun --args='--server.port=18080 --spring.r2dbc.url=r2dbc:h2:mem:///oms_ledger_smoke;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE --spring.r2dbc.username=sa --spring.r2dbc.password= --oms.sync.scheduler.enabled=false --oms.naver.real.enabled=false --oms.coupang.real.enabled=false --oms.mock-data.enabled=false'
```

Then:

```bash
curl POST http://localhost:18080/api/auth/login
curl POST http://localhost:18080/api/ledger/imports
curl GET  http://localhost:18080/api/ledger/imports/{importId}
curl GET  http://localhost:18080/api/ledger/imports/{importId}/sources
```

Result: PASS.

- Uploaded files:
  - `Statistics-20260418~20260418_(0).xlsx`
  - `inventory_health_sku_info_20260616220816.xlsx`
  - extension-generated `A01549099-dailySettlement-20260418-20260418.xlsx`
- Response:
  - `status`: `PARSED`
  - `importId`: `LI-DD882E881F9445EBA641910257F6243C`
  - `targetDate`: `2026-04-18`
  - `batchName`: `2026-04-18-extension-smoke`
  - `totalSalesAmount`: `22000`
  - `totalSalesQuantity`: `2`
  - `totalAdSpend`: `1209`
- File parse statuses:
  - `COUPANG_SALES_STATISTICS`: `PARSED`, row count `1`
  - `COUPANG_INVENTORY_HEALTH`: `PARSED`, row count `6`
  - `COUPANG_DAILY_SETTLEMENT`: `PARSED`, row count `2`
- Follow-up reads:
  - `GET /api/ledger/imports/{importId}` returned `PARSED`
  - `GET /api/ledger/imports/{importId}/sources` returned 3 source workbooks

## Continue Real-Site Verification

To continue real selector discovery:

```bash
cd /Users/admin/refacoring_project/saiload/onchak-extension
npm run build
npm run playwright:abrs:profile
```

Then log in to Coupang Wing in the opened Playwright browser. The same profile directory will be reused in later sessions.

## Notes

- Inventory and sales auto fetch are implemented from the active Wing tab.
- Daily settlement auto fetch is implemented from the active Coupang Ads tab.
- Because inventory/sales and daily settlement live on different Coupang hosts, the user currently needs the relevant host tab active before pressing that row's auto-download button.
- `test-results/` is ignored and stores local screenshots/download evidence only.
