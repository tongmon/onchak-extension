import { createTheme } from '@mantine/core';

export const extensionTheme = createTheme({
  primaryColor: 'teal',
  defaultRadius: 'lg',
  fontFamily:
    'Aptos, "Segoe UI Variable", "Trebuchet MS", -apple-system, BlinkMacSystemFont, sans-serif',
  headings: {
    fontFamily:
      '"Aptos Display", Aptos, "Segoe UI Variable", "Trebuchet MS", sans-serif',
  },
});

