export {
  DEFAULT_EXCHANGE_RATE,
  defaultExtensionSettings,
  type ExtensionColorScheme,
  type ExtensionSettings,
  type ProductionCostCurrency,
} from '@/shared/extension';
export { useExtensionSettingsStore } from './model/settings-store';
